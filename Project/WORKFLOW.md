You give research topic -> goes to first agent search agent(AgentExecutor +
web_search_tool) search agent will be linked(basically fetch the realtime data and send back to search agent) with the tool1 tavily api- live web results. this agent will store all this to state memory state["search result"] saved this is now forwarded to second agent that is reader agent (AgentExecutor + scrape_url_tool) this will fetch more info from tool2 BeautifulSoup scrapes pages now this is also stored in the shared memory state['scraped content'] saved !! now two chains will be created (people call it agents but they are chains basically ) - writer chain and critic chain they get the output from the shared memory and generate final output 
